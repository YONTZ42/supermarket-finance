import { DataEntryWorkspace } from "@/src/features/data-entry/components/DataEntryWorkspace";

export default function DataEntryPage() {
  return <DataEntryWorkspace initialStoreCode="TOKYO" initialFiscalYear={2022} />;
}
